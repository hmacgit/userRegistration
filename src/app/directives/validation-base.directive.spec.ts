import { ValidationBaseDirective } from './validation-base.directive';

describe('ValidationBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationBaseDirective();
    expect(directive).toBeTruthy();
  });
});
